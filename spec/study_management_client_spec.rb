require "spec_helper"

describe StudyManagementClient do
  it "has a version number" do
    expect(StudyManagementClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
